import React from 'react';
import {Text, View} from "react-native";

const Surname = ({name}) => {
    return (
        <View>
            <Text>{name}</Text>
        </View>
    );
};

export default Surname;